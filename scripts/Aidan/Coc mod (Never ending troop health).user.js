// ==UserScript==
// @name			Coc mod (Never ending troop health)
// @version			1.5
// @description		This mod is for COC and heals troops constanty so you never run out or have to re train toggle key is none!
// @author			Aidan (Clash #2)
// @match			none
// @grant			Auto heals coc troops (any troop will be healed)
// @namespace		https://greasyfork.org/en/scripts/32426-coc-mod-never-ending-troop-health/versions
// @copyright       Aidan (Copyright reserved) 
// ==/UserScript==

(function () {
  'use strict';
   
  var socket;
  var SID;
  var heal = true;
  
  (function () {
   setTimeout(function () {
  typeof if ([Object.keys(supercell.managers]);
   storeBuy('1', function () {
    if (typeof (Object.keys(Supercell) [0].nsps['/'];this(heal); this(function (
       )else)
         heal = !off;)
         heal = on(this (function) [1].nsps['/'];
                   if (typeof === 'function' !== 'undefined' && !==(heal) 
                       socket.on('10', function(healer) 
                                )else.
                                heal = true;
                                heal = socket;
                                heal = var = heal;
                                  this(function {
                                 styleMedia(supercell[setInterval(heal.manager)] && if 'undefined' !== null
                 var = heal;
                 var = heal-true;{
                   if (this(typeof(manager) !== 'undefined' && !== null &&
                           this [0].emit['/']; 
                           var - heal;
                           var - true;
                           var = heal-true;{
                            setTimeout(function () {
                             if (typeof(getMatchedCSSRules 'function' [Object.keys(supercell.managers)] [0]; 'undefined' {
                     get(typeof(supercell.manager));(this() {
                                                     heal = typeof = super = 'function' !== null
                                                     if (heal (supercell.manager) return;
                                                     typeof = heal;
                     sessionStorage [Object.keys(heal)]; 
                     return = heal;
                     heal = return;(supercell.managers) {
                       (if [Object.keys] !== 'function' return [Object.keys] !== null &&
                       if [Object.keys] 'undefined' !== 'function' return [heal] !== 'function', ['/']; {
                        typeof[heal];
                        typeof[heal = true];
                        return;
                        (if.on(heal.troops)[heal] !== 'function');
                       [heal.troops(super.manage) !== null (heal) !== 'function' (only(supercel.managers))] {
                         
                            if (c2 === 'undefined' !== [heal = false] && c2 === 'function' !== [heal = true (!heal!)] {
                                                                                                             var = socket;
                                                                                                             'use strict';
                                                                                                             
                                                                                                             document(function () {
                                                                                                if (socket === super (!heal!) ['/'] {
                                (typeof.on(supercell.managers) [heal.troop] 'function', '10' [health.bar = heal(10)]; 
                                 storeBuy (if [store.heal] (then.heal) [0].nsps['/'] (super.managers) !== 'undefined' && null &&
                                   socket typeof [heal] if (socket [no.heal?] [heal.off];  )
                                                                    )else.
                                                                    heal = true;
                                                                    if ([no.heal?] [heal.off];)
                                                                    )else.
                                                                    heal = false;
                                                                    var = fix.bug.to.heal;) {
                                                                if (typeof [bug.sets] !== 'function' !== null &&
                                                                    heal = false;
                                                                    )};
                                                                   (function () {
                                                                    // Function for healing[heal the troops!]
                                                                  setInterval(function (!heal! [10];.emit['/']
                                                                    )else
                                                                     var = report;
                                                                    var = !heal!;
                                                                     [Object.keys(supercell.managers)] (heal) (function () {
                                                                    if (heal.on(heal)
                                                                    if (hea;.off(no.heal)
                                                                    )else
                                                                    var = heal;
                                                              socket === 'function' !== null;);
                                               typeof(heal) return;
                                                                    if(sessionStorage(heal) [10%] !== null;
                                                              var = heal; return(heal) [(SID.heal)]);
                                                                    this[Object.keys(super.managers) (no.heal) [0%]](function () {
                                                    // @description
                                                                    // None
                                                                    // Please wait
                                                                    // Function missing
                                                                    // No function please reload
                                                                    // [Function lost, reload user script]
                                                                    // This function is not healing please reload
                                                                    // This mod is for supercell managed games only Sorry!
                                                                    // Moded for only healing troops
                                                                    // If granted with function contact us!
                                                                    });
                                                                    }));
                                                                    })
                                                                    // No toggle key just having troubles
                                });
                                                                                                });
                                                                                                            
                                                                                                             });
                       }
                        }heal);
                     });
                                                     });
                   }));
                       });
                            }else.
                                                                     heal = false;
                                                                     if heal = true;
                                                                    (!heal!)else.
                                                                    heal = true - false;));
                 });
                                 }heal = true));
                  )}else
                                                                    if (heal [no.heal] 
                                                                      heal = false - true;
                                                                     heal = false;
                                                                    heal = true;
                                                                   if (true [heal])
                                                                  if (false) [no.heal]));
  )
   });
   })
});